#!/usr/bin/env python3
from sys import argv
import os


if len(argv) != 5:
    exit(1)

a, b, newa, newb = argv[1:]
print(a, b, newa, newb)


if not os.path.isdir(newa):
    os.mkdir(newa)
if not os.path.isdir(newb):
    os.mkdir(newb)


